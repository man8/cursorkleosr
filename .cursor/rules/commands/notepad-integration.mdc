---
description: "Notepad Integration for Command Execution"
globs: "**/*"
tags: [notepad, integration, commands]
priority: 1
version: 1.0.0
---
# Notepad Integration

Integration between command system and Cursor notepad for interactive workflows.

## Features

- Command execution with `@notepad:` prefix
- Structured output display
- Command history tracking
- Session management

## Commands

- `@notepad:analyze` - Run analysis in notepad
- `@notepad:blueprint` - Create blueprint in notepad
- `@notepad:validate` - Run validation in notepad
- `@notepad:thinking` - Run sequential thinking in notepad
- `@notepad:history` - View command history
- `@notepad:session:start` - Start new session
- `@notepad:session:end` - End current session
- `@notepad:session:view` - View session details

## Usage

1. Open Cursor notepad
2. Type command with `@notepad:` prefix
3. Press Enter to execute
4. View structured results

## References

- See `.cursorrules` for complete command list
- See `commands/command-system.mdc` for command system details 